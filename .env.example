# React contact form → PHPMailer endpoint
# Localhost / same domain: relative path is enough
REACT_APP_CONTACT_API_URL=/api/send-mail.php

# If PHP is deployed elsewhere (e.g. https://site.com/api/send-mail.php), override:
# REACT_APP_CONTACT_API_URL=https://site.com/api/send-mail.php
